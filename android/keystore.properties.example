# Copy to android/keystore.properties (NEVER commit real passwords).
# Paths are relative to the android/ folder.

storeFile=../secrets/ilaria-release.jks
storePassword=CHANGE_ME_STORE
keyAlias=ilaria
keyPassword=CHANGE_ME_KEY
